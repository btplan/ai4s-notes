## 概念
1. DFT
   密度泛函理论，方法家族，预测材料性质
   影响因素：方法 / 参数
2. 大家实现PBE时 结果一致吗？
3. 用一套高精度 all-electron 参考数据，去检验其他 DFT 代码和赝势方案算出来的 EOS 是否一致。
## Problem
如何用一套可重复、通用、自动化的流程，验证不同 DFT 实现的数值精度？
用一套高精度 all-electron 参考数据，去检验其他 DFT 代码和赝势方案算出来的 EOS 是否一致。
我的 DFT 实现是否能复现一个高精度参考结果？
## SOTA：如何评价DFT质量
**Δ metric** 两条EOS曲线的距离
### 不足
1. 材料集合太小
2. 化学环境不够丰富
3. 指标有缺陷
改进目标：扩展材料集合，建立系统数据集 提出新指标衡量

## Methods
### 1. 评价对象

  DFT 代码计算 EOS 的能力。
  对每个晶体，计算 7 个不同体积下的总能量，然后拟合出 EOS 曲线，再比较不同代码得到的曲线和参数。
  晶体结构 → 改变体积 → 计算每个体积的能量 → 拟合 EOS 曲线 → 得到 V₀、B₀、B₁ → 与参考结果比较。
### 2. 参考数据集构建
- 全电子方法
- 用两种代码验证参考数据集可靠新

### 3. 材料选择
元素种类更多 每个元素的结构更多（单质、氧化物），模拟不同化学环境
可迁移性：当元素进入不同配位数、不同氧化态、不同键长的环境时，DFT 实现是否仍然稳定可靠

#### 材料结构并不真实也没关系？
目的：不关系预测真实材料，做verification

同一个理想测试题，不同 DFT 代码能不能给出一致答案？

### 4. 如何计算EOS
选取不同体积下的7组数据
它把离散的 7 个能量点拟合成一条平滑曲线，然后从曲线中提取平衡体积和抗压性质。

### 5. 如何比较两个 DFT 结果
#### 5.1 直接比较 EOS 参数
也就是比较两个代码得到的：
- V₀ 差多少；
- B₀ 差多少；
- B₁ 差多少。
图 1 就是这么做的。它比较 FLEUR 和 WIEN2k 两个 all-electron 代码的 V₀、B₀、B₁ 差异分布。
结论是：两个 all-electron 代码高度一致，所以它们的平均值可以作为参考数据集

#### 5.2 旧指标 delta
Δ 衡量两条 EOS 曲线的整体差异。

优点：一个数就能表示两条曲线差多远。

缺点：它对材料的体模量 B₀ 敏感，不同材料之间横向比较不够公平。

#### 5.3 新指标 ε 和 ν
##### 5.3.1 ε
一个无量纲的、归一化的 EOS 曲线差异指标
> 它比较两条 E(V) 曲线形状是否一致，而且尽量减少材料本身“软硬程度”对指标的影响。

- 不依赖能量单位；
- 不太受 bulk modulus 大小影响；
- 适合跨不同结构、不同化学环境比较；
- 可以直接衡量两条 EOS 曲线整体是否相似

##### 5.3.2 ν
ν 直接比较 V₀、B₀、B₁ 三个物理参数的相对差异，并给它们加权成一个综合分数

- V₀ 权重大；
- B₀ 权重较小；
- B₁ 权重更小。

为什么？

因为 B₀ 和 B₁ 是从曲线的更高阶变化中来的，对噪声更敏感。如果把它们和 V₀ 同等看待，会过度放大不稳定误差。

所以 ν 是一个更物理、更稳健的综合评价指标。

论文还给出经验阈值：

|评价|ε|ν|
|---|---|---|
|excellent agreement|ε ≲ 0.06|ν ≲ 0.1|
|good agreement|ε ≲ 0.2|ν ≲ 0.33|

这就是论文直接给出的“DFT 质量评价标准”之一。
### 6. 控制变量设定：如何保证公平

- 使用 PBE exchange-correlation functional；
- 不考虑自旋极化；
- 使用 scalar-relativistic treatment；
- 使用相同 smearing；
- 使用相同 k-point mesh 规则；
- 用相同体积范围；
- 用相同 EOS 拟合协议。

其是 smearing 和 k-point，论文反复强调非常关键。

>什么是 k-point?

晶体是周期性结构，电子状态需要在倒空间中采样。k-point 可以理解成“采样点”。采样越密，结果通常越稳定，但计算越贵。

>什么是 smearing？

在金属或复杂电子结构中，电子占据会比较难处理。smearing 是一种数值平滑技巧，可以帮助计算收敛。
但是 smearing 不是无害的。论文图 3 展示了一个极端例子：不同 smearing 类型和宽度会让 Er diamond 结构的 EOS 出现不同最低点。也就是说，如果 smearing 不一致，你以为是在比较代码，其实是在比较不同数值设置。

所以论文的方法优势之一就是：


> 它不仅给结果，还明确规定了如何复现和比较这些结果。
